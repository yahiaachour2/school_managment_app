
export interface CreateuserSubjectNote {
  userId: string;
  subjectId: string;
  note: number;
}

export interface GetuserSubjectNote {
    id: string;
 
  }


  export interface UpdateuserSubjectNote {
    id: string;
      userId: string;
  subjectId: string;
  note: number;
 
  }
  
  