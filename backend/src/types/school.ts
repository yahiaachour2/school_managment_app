export interface CreateSchoolInput {
  name: string;
  address: string;
}
export interface getSchoolInput {
  id: string;
}

export interface updateSchoolInput {
  id: string;
  name: string;
  address: string;
}

export interface deleteSchoolInput {
    id: string;
   
  }
  
